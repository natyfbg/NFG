# .env.example
# Copy to ".env" and adjust as needed.

# ---------------------------------------------------------
# Local development (run: python app.py)
# ---------------------------------------------------------
# Use your locally running MongoDB:
MONGO_URI=mongodb://localhost:27017/NFG

# Flask secret for sessions/CSRF (set to a strong random value in real use)
SECRET_KEY=dev-secret

# Or use MongoDB Atlas (uncomment and edit below)
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/NFG
